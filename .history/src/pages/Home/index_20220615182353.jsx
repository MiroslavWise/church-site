import {Alert} from 'bootstrap'

export default () => {

        return(
                <>
                        <Alert variant={'primary'} className="cursor-pointer">1234</Alert>
                </>
        )
}